if books != null
    each book of books 
        a(href="/books/"+book.isbn) #{book.title}
        if user.owner
            button#book.isbn(type='button' onclick='removeFromStore(this.id)') Remove
        else
            button(type='button' id=book.isbn onclick='addToCart(this.id)') Add To Cart
        br